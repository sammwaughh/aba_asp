# Handwritten exhaustive root-support tables

These CSVs are the learner-visible tables for M2-S1. Each file lists every
assignment of the **root** variables, with non-roots filled by the deterministic
mechanism. They are not IID samples.

Copied into each generated fixture bundle as `samples/support.csv`.
