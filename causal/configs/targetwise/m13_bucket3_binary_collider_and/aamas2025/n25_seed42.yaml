description: "H1 nested-prefix AAMAS collection over n25_seed42 (missing (0,0,0) rows)."

configuration:
  id: aamas2025

fixture:
  directory: causal/outputs/causal_fixtures/m13_bucket3_binary_collider_and
  sample: samples/n25_seed42.csv

encoding:
  type: exact_value
  example_policy: binary_one_vs_zero

learner:
  prolog_config: configs/aamas2025_config.pl
  prolog_timeout_s: 300
  joint_check_timeout_s: 5
