description: "H4 cautious baseline on the closed binary AND-collider fixture and frozen n30_seed42 sample. Infrastructure-ready; not yet run."

configuration:
  id: baseline_cautious

fixture:
  directory: causal/outputs/causal_fixtures/m13_bucket3_binary_collider_and
  sample: samples/n30_seed42.csv

encoding:
  type: exact_value
  example_policy: binary_one_vs_zero

learner:
  prolog_config: configs/baseline_cautious_config.pl
  prolog_timeout_s: 300
  joint_check_timeout_s: 5
