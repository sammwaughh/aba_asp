# M1.3 — Bucket 3 claims (planning record)

## Status

`direction approved; H0 closed; H1–H2 run / analysed; H3–H4 deferred` —
opened 2026-07-22 and narrowed following Fabrizio's 31 July feedback. Baseline
**H0** is **closed**. Probes **H1** and **H2** are **run / analysed**. Next work
is deferred probes H3–H4 (`future_probes.*`). **No Bucket 3 claim** is approved.
There is no approved run matrix.

Investigation: **M13-C3** —
`docs/experiments/qualitative/M13-C3-binary-collider-and/experiment.md`
(H0: `learning_analysis.md`; H1: `h1_support_ablation.md`; H2:
`h2_irrelevant_covariate.md`). Still **no Bucket 3 claim**.

Bucket 3 extends Milestone 1 / M1.3. It does not replace or reopen M12x, Bucket 1, or
Bucket 2. Those records remain locked evidence. Milestone 2 has not started.

## Supervisor-defined scope

The planning process must consider:

- controlled variation of the learning target and movement towards target-agnostic
  analysis of when target-wise outputs support recovery of causal structure;
- controlled variation of data availability, with the exact meaning of missing data
  (rows, cell values, variables, or combinations) still to be decided;
- larger graph fixtures, motivated in part by the four-node U7 diamond;
- explicit graph–mechanism validity and clear statements of support;
- explicit analysis of correlation, marginal independence, conditional independence,
  and Markov equivalence classes;
- separation of failures caused by a specified ABA Learning strategy from limitations
  caused by observational equivalence, missing information, representation, or data;
- possible variation in category count and possible non-discrete extensions.

These dimensions remain possible Bucket 3 work. They are currently deferred while the
deterministic-mechanism foundation below is developed one bounded case at a time.

## 31 July immediate direction

The next investigations will use binary causal models with:

- mutually independent, non-degenerate stochastic root distributions;
- deterministic non-root structural functions;
- a target-free IID table sampled through the roots and deterministic propagation;
- all variables considered separately as learning targets on the same frozen table; and
- a pre-run evaluator-only reference against which learned frameworks can later be
  described.

Future fixtures use lowercase internal identifiers `a`, `b`, `c`, ... for YAML, CSV,
ABA predicates, targets, and paths, while prose and diagrams may use (A,B,C,\ldots).
Existing `xN` fixtures and evidence remain unchanged. ABA learner symbols `alpha_N` and
`c_alpha_N` occupy a separate reserved namespace.

For each fixture, the reference must record:

- root distributions and complete deterministic truth tables;
- formal, population-supported, and finite-sample-observed parent configurations;
- canonical exact-value positive-state rules where the current binary encoding applies;
- that a root target has no deterministic causal rule over other observed variables; and
- that semantic equivalence cannot be reduced to exact rule-string equality.

The generating graph, population certificate, support information, and mechanism
reference are evaluator-only. They must not enter learner-visible BK or examples.

Every fixture still requires an exact population-level audit. Root stochasticity,
deterministic propagation, active edges, and DAG factorisation do not individually or
jointly guarantee ordinary faithfulness.

## First approved investigation (H0 closed; H1–H2 analysed; no claim)

| Field | Value |
|---|---|
| Experiment ID | `M13-C3` |
| Baseline label | **H0** (closed) |
| Fixture ID | `m13_bucket3_binary_collider_and` |
| Display / internal | \(A,B,C\) / `a`, `b`, `c` |
| DAG | \(A \rightarrow C \leftarrow B\) |
| Mechanisms | \(A\sim\mathrm{Bernoulli}(0.8)\), \(B\sim\mathrm{Bernoulli}(0.7)\), independent; \(C=A\land B\) |
| Frozen sample | \(n=30\), seed `42` (approved) |
| Evidence record | `docs/experiments/qualitative/M13-C3-binary-collider-and/experiment.md` |
| Working LaTeX dossier | `.../M13-C3-binary-collider-and/fixture_dossier.tex` |
| H0 learning record | `learning_analysis.md` / `.tex` (AAMAS + ECAI six-cell; closed) |
| H1 record | `h1_support_ablation.md` / `.tex` (**run / analysed**; not a claim) |
| H2 record | `h2_irrelevant_covariate.md` / `.tex` (**run / analysed**; not a claim) |
| Claim status | **none** |
| Future probes | `future_probes.md` (+ `.tex`); H1–H2 complete; H3 design approved; H4 proposed/infra-blocked; **not claims** |

Pre-run artefacts:
`causal/outputs/causal_fixtures/m13_bucket3_binary_collider_and/`.
Target-wise outputs:
`causal/outputs/aba_learning/targetwise/m13_bucket3_binary_collider_and/aamas2025/{n30_seed42,n25_seed42}/`
and `.../ecai2024/n30_seed42/`.

## Preserved pre-pivot artefacts

The positive-stochastic fixture
`causal/fixtures/specs/m13_bucket3_binary_diamond.yaml`, its generated bundle, its
`n50_seed42` sample, and its AAMAS 2025/ECAI 2024 target-wise outputs remain unchanged for
provenance. They established and exercised the fixture and all-target infrastructure
before the 31 July pivot. They are not reinterpreted as deterministic evidence and do not
currently support a Bucket 3 claim.

## Open theory point

For an isolated two-node copy relation (A \to B, B:=A), observational dependence does
not distinguish the generating orientation from (B \to A). Additional variables may
provide orienting information through the larger graph, but this is fixture-dependent and
is not assumed in advance.

## Planning guardrails

- Read and verify the relevant canonical causal theory before using independence or
  Markov-equivalence statements in fixture design.
- Treat Fabrizio's fork/chain conditioning example as a question to formalise and
  verify, not yet as a project claim.
- Do not describe the current target-wise bridge as full causal discovery or
  Russo-style Causal ABA.
- Use small exploratory cells to understand each variation before defining targeted
  claim-supporting experiments.
- Keep exploratory observations distinct from evidence-backed claims.
- Samuel owns research decisions; ChatGPT and Cursor may organise, formalise, inspect,
  implement, and critically review the supervisor-supplied direction.

## Inputs preserved from completed work

- M12x Stage-3 inspection:
  `docs/experiments/qualitative/M1.2-expanded-cell-inspection.md`
- Bucket 1 (locked):
  `docs/experiments/qualitative/M1.3-bucket1-claims.md`
- Bucket 2 (locked):
  `docs/experiments/qualitative/M1.3-bucket2-claims.md`
- M13-C1:
  `docs/experiments/qualitative/M13-C1-causal-role-underdetermination/experiment.md`
- M13-C2:
  `docs/experiments/qualitative/M13-C2-bk-feature-order/experiment.md`
- Supervisor guidance:
  `docs/research/supervisor_guidance.md`

## Next step

**H0 is closed. H1 and H2 are run / analysed.** Next: open deferred probes
H3–H4 consistently with `future_probes.md`. Do not invent a Bucket 3 claim or
expand to a run matrix before inspected probe evidence.
