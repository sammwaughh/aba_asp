# <EXPERIMENT_ID> — <Short title>

## Status

`proposed | planned | implemented | run | analysed | reported`

Current status: `proposed`

## Purpose

State, in one short paragraph, what this experiment is for.

This should explain why the experiment is being run, but it should not overstate what it can show. If the experiment uses the current `aba_asp/causal` bridge, describe it as an ABA Learning-based parent-set or learned-rule investigation unless the implementation directly uses Russo-style Causal ABA machinery.

## Research question

State the precise question.

Example form:

> Under <specified data-generating structure>, when the current ABA Learning bridge is trained target-by-target, do the learned target rules recover <specified structure>, and under what failure modes?

## Theoretical motivation

Explain why the experiment matters in relation to the project.

Separate the following explicitly:

- **ABA foundations:** assumptions, contraries, attacks, extensions, and accepted claims.
- **ABA Learning / ABALearn:** transformation-rule learning from background knowledge and examples.
- **Causal ABA / argumentative causal discovery:** Russo-style representation of causal discovery using assumptions such as arrows, no-edges, independences, d-separation, and stable extensions corresponding to candidate DAGs.
- **Current `aba_asp/causal` implementation:** the actual code path used in this experiment.

## Relation to ABA Learning

Describe how the experiment uses ABA Learning.

Include, if applicable:

- background knowledge construction;
- positive and negative examples;
- target predicate;
- learned delta rules;
- Rote Learning;
- Folding;
- Assumption Introduction;
- learner strategy or configuration.

## Relation to Causal ABA

State what the experiment does and does not test.

Use this distinction:

- This experiment may test whether learned ABA rules recover causal-parent-like structure from data generated by a known causal graph.
- This experiment does **not** test full Russo-style Causal ABA unless it explicitly uses arrow/no-edge/independence assumptions, d-separation reasoning, and stable extensions as candidate DAGs.

## Implementation scope

### Code/config paths

- Config path: `TBD`
- Runner/script path: `TBD`
- Metrics path: `TBD`
- Output path: `TBD`
- Figure/table path: `TBD`

### Files changed

List files changed for this experiment.

- `TBD`

### Files inspected but not changed

List important files inspected.

- `TBD`

## Dataset / data-generating process

### Source

- `ArgCausalDisco | handcrafted fixture | other`
- Exact generator/function: `TBD`

### Graph / structure

Specify the graph used.

```text
TBD
```

### Variables

| Variable | Meaning | Notes |
|---|---|---|
| `x0` | TBD | TBD |
| `x1` | TBD | TBD |
| `x2` | TBD | TBD |

### Sample sizes

- `TBD`

### Seeds

- `TBD`

### Noise / stochasticity

Describe what is known about stochasticity in the generated data.

If unknown, write:

> The precise noise mechanism was not assumed; it was inspected through the generated samples and the generator code before interpreting the learner output.

## Encoding

Describe how tabular data is converted into ABA predicates and examples.

### Target variable(s)

| Target | True parents | Target type | Reason for inclusion |
|---|---:|---|---|
| `TBD` | `TBD` | root / has-parents / collider / other | TBD |

### Positive examples

Describe how `E+` is constructed.

```text
TBD
```

### Negative examples

Describe how `E-` is constructed.

```text
TBD
```

### Background knowledge

Describe the generated BK at a high level.

- Target excluded from BK: `yes | no | TBD`
- Binary features encoded as: `TBD`
- Non-binary discrete features encoded as: `TBD`
- Continuous features encoded as: `TBD`

## Commands run

Record exact commands.

```bash
TBD
```

## Environment

Record only facts relevant to reproducibility.

- Conda environment: `TBD`
- Python version: `TBD`
- SWI-Prolog version: `TBD`
- clingo version: `TBD`
- Git commit: `TBD`
- Dirty tree: `TBD`

## Artefact paths

| Artefact | Path | Notes |
|---|---|---|
| Results parquet | `TBD` | TBD |
| Manifest | `TBD` | TBD |
| Run log | `TBD` | TBD |
| Cell outputs | `TBD` | TBD |
| Figures | `TBD` | TBD |
| Summary markdown | `TBD` | TBD |

## Outcome summary

Give a factual summary of what happened.

Do not interpret yet.

| Outcome | Count | Notes |
|---|---:|---|
| solved | TBD | TBD |
| completed_empty_delta | TBD | TBD |
| completed_no_solution | TBD | TBD |
| timeout | TBD | TBD |
| skipped | TBD | TBD |
| error | TBD | TBD |

## Quantitative results

Only include metrics that were actually computed.

| Grouping | Solve fraction | Median body-parent F1 | Offgraph rate | Timeout rate | Notes |
|---|---:|---:|---:|---:|---|
| TBD | TBD | TBD | TBD | TBD | TBD |

### Metric interpretation

Explain what the metrics mean in this experiment.

- `body_parent_precision`: TBD
- `body_parent_recall`: TBD
- `body_parent_f1`: TBD
- `offgraph_rate`: TBD
- `ancestor_only_rate`: TBD
- `cov_py_*`: TBD
- `cov_pl_*`: TBD
- `cov_gap_*`: TBD
- Efficiency metric(s): TBD

## Qualitative learned-rule inspection

Use selected cells to narrate what the learner did.

### Case 1

- DGP: `TBD`
- Target: `TBD`
- True parents: `TBD`
- Expected learned body, if parent recovery succeeds: `TBD`
- Outcome: `TBD`

Learned target rule(s):

```text
TBD
```

Body variables:

```text
TBD
```

Classification:

`parent match | missing parent | extra ancestor | child/descendant | off-graph | empty delta | no solution | timeout | parser issue | other`

Interpretation note:

```text
TBD
```

### Case 2

- DGP: `TBD`
- Target: `TBD`
- True parents: `TBD`
- Expected learned body, if parent recovery succeeds: `TBD`
- Outcome: `TBD`

Learned target rule(s):

```text
TBD
```

Body variables:

```text
TBD
```

Classification:

`parent match | missing parent | extra ancestor | child/descendant | off-graph | empty delta | no solution | timeout | parser issue | other`

Interpretation note:

```text
TBD
```

## Failure modes

List observed failure modes.

| Failure mode | Evidence | Likely layer | Notes |
|---|---|---|---|
| TBD | TBD | data generation / encoding / learner / metrics / environment / interpretation | TBD |

## Interpretation notes

Write bounded interpretation.

Separate facts from interpretation.

### Facts

- `TBD`

### Interpretation

- `TBD`

### Uncertainties

- `TBD`

## Claims supported

List only claims supported by this experiment.

- `TBD`

## Claims not supported

List tempting claims that must not be made.

- `TBD`

Example claims not to make unless directly supported:

- “The current implementation performs full Causal ABA.”
- “The experiment proves causal discovery.”
- “The learned rule direction is necessarily causal.”
- “The method recovers the full DAG.”
- “Noise is always represented as ABA exceptions.”

## Report relevance

State where this belongs in the interim or final report.

- Interim report section: `TBD`
- Possible figure/table: `TBD`
- Possible narrative role: `TBD`

## Next decision

State what this experiment implies for the next step.

Options:

- proceed to a strategy comparison;
- inspect encoding;
- instrument additional metrics;
- extend to larger motifs;
- revise interpretation;
- stop this line of experimentation.

Decision: `TBD`

## Cursor handoff notes

Record what Cursor should do next, if anything.

```text
TBD
```

## ChatGPT handoff notes

Record what should be uploaded or pasted into ChatGPT for report drafting.

```text
TBD
```

## Samuel review checklist

Before using this experiment in the report, Samuel should verify:

- [ ] Commands are exact.
- [ ] Artefact paths exist.
- [ ] Results match the copied tables.
- [ ] Learned-rule examples are copied accurately.
- [ ] Metrics are interpreted correctly.
- [ ] Unsupported claims are explicitly excluded.
- [ ] The ABA Learning / Causal ABA distinction is preserved.
- [ ] The report claim is modest enough for the evidence.