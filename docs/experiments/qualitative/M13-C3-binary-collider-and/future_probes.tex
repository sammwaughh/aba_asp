\documentclass[11pt]{article}
\usepackage[a4paper,margin=25mm]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,booktabs}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0.55em}
\setlist{nosep,leftmargin=*}
\emergencystretch=3em

\title{M13-C3 signposted future probes\\
(H1--H3)}
\author{}
\date{H1--H3 designs approved for investigation\\
(not yet implemented / not tested / not claims)}

\begin{document}
\maketitle

\noindent\textbf{Status.} Near-term probes for the M13-C3 target-wise
investigation (\texttt{m13\_bucket3\_binary\_collider\_and}).
H1/H2/H3: \textbf{approved} (H3: design approved) / not yet implemented /
not tested.
\textbf{Not} Bucket~3 claims; \textbf{not} locked Bucket~1/2 claim content;
\textbf{not} report-facing prose.
Markdown twin: \texttt{future\_probes.md}.

\noindent\textbf{Samuel decisions (2026-08-01).}
\begin{enumerate}
  \item \textbf{H1 approved:} nested-prefix / missing-\((0,0,0)\) AAMAS ablation
    (e.g.\ \(n{=}25\), seed~42, targets \(a,b\)) vs \(n{=}30\) baseline.
  \item \textbf{H2 design approved:} isolated \(D\sim\mathrm{Bernoulli}(1/2)\)
    on \(A\to C\leftarrow B\); H2c in scope; intended id
    \texttt{m13\_bucket3\_binary\_collider\_and\_iso\_d}.
  \item \textbf{H3 design approved:}
    id \texttt{m13\_bucket3\_binary\_bd\_and\_lead\_a};
    DAG \(B\to C\leftarrow D\), isolated \(A\sim\mathrm{Bernoulli}(1/2)\);
    \(B\sim\mathrm{Bernoulli}(4/5)\), \(D\sim\mathrm{Bernoulli}(7/10)\);
    primary ECAI on target \(c\); \textbf{AAMAS contrast in scope};
    planned sample \texttt{n30\_seed42}.
\end{enumerate}

\section{Motivation (AAMAS --- H1/H2; ECAI --- H3)}

AAMAS on \texttt{n30\_seed42}: \(c\) solved with
\texttt{c(A) :- a\_val\_1(A), b\_val\_1(A).}; roots
\texttt{completed\_no\_solution} via both-0 over-generalisation blocked by
rows~26 and~30.
ECAI on the same sample, target \(c\): first nd fold matches BK-leading
\texttt{a\_val\_1}; delta retains \(a\) with assumption/contrary repair.
Full artefact paths: see \texttt{future\_probes.md}.

\section{Ordering and relation}

\begin{enumerate}
  \item H1 (existing three-variable fixture, AAMAS).
  \item H2 (\texttt{...\_iso\_d}, AAMAS; trailing distractor \(D\)).
  \item H3 (\texttt{...\_bd\_and\_lead\_a}, ECAI primary + AAMAS contrast;
    leading distractor \(A\), parents \(B,D\)).
\end{enumerate}

\begin{center}
\begin{tabular}{@{}lp{0.62\textwidth}@{}}
\toprule
Probe & Focus \\
\midrule
H1 & AAMAS roots; missing negative witness on current fixture \\
H2 & AAMAS \(c\); greedy maximal bodies include trailing \(D\) \\
H3 & ECAI \(c\) first fold on leading \(A\); AAMAS contrast in scope \\
\bottomrule
\end{tabular}
\end{center}

\section{Probe family H1}

Approved nested-prefix / missing-\((0,0,0)\) AAMAS ablation on
\texttt{m13\_bucket3\_binary\_collider\_and}.
Predicts spurious both-0 root rules when rows~26/30 absent; on \(n{=}30\),
\texttt{completed\_no\_solution}. Roots have no deterministic mechanism rule.

\section{Probe family H2}

Design approved: \(A\to C\leftarrow B\) plus isolated \(D\sim\mathrm{Bernoulli}(1/2)\);
intended id \texttt{m13\_bucket3\_binary\_collider\_and\_iso\_d}.
AAMAS: greedy bodies predicted to include \texttt{d\_val\_*}; H2c in scope.
Do not collide with H3 id.

\section{Probe family H3 --- ECAI first-fold distraction by BK-leading \(A\)}

Status: design approved / not yet implemented / not tested / not a claim.
Id: \texttt{m13\_bucket3\_binary\_bd\_and\_lead\_a}.

\subsection*{Approved design}
DAG: \(B\to C\leftarrow D\); no edges involving \(A\).
\begin{align*}
A &\sim \mathrm{Bernoulli}(1/2),\\
B &\sim \mathrm{Bernoulli}(4/5)
\quad (P(B{=}0)=1/5,\ P(B{=}1)=4/5),\\
D &\sim \mathrm{Bernoulli}(7/10)
\quad (P(D{=}0)=3/10,\ P(D{=}1)=7/10),\\
C &= B \land D.
\end{align*}
Evaluator reference for \(c\):
\texttt{c(A) :- b\_val\_1(A), d\_val\_1(A).}
(\(a\) omitted).
BK order for target \(c\): \(a\), then \(b\), then \(d\).
Planned sample: \texttt{n30\_seed42}.
Primary arm: ECAI target \(c\).
Secondary arm: AAMAS on the same sample (\textbf{in scope}).

\subsection*{Working hypotheses (not claims)}
ECAI: first nd fold predicted to match \texttt{a\_val\_*}; repair may solve
while retaining \(a\) in the ABA presentation.
AAMAS: greedy \(E^+\) bodies with \(C{=}1\) predicted to include the
co-occurring \texttt{a\_val\_*} (related to but distinct from H2).

\subsection*{Planned investigation}
Author/certify the fixture; run ECAI (primary) and AAMAS (contrast); inspect
target \(c\) against the evaluator reference. No expanded matrix without
further approval.

\section{Decisions recorded}

\begin{center}
\begin{tabular}{@{}llp{0.22\textwidth}@{}}
\toprule
Item & Decision & Date \\
\midrule
H1 nested-prefix AAMAS ablation & Approved & 2026-08-01 \\
H2 isolated-\(D\) design; H2c & Approved / in scope & 2026-08-01 \\
H3 design, id, \(B\)/\(D\) odds, AAMAS contrast & Design approved & 2026-08-01 \\
\bottomrule
\end{tabular}
\end{center}

Still no Bucket~3 claim. Implementation remains for Fixture/target-wise agents.

\end{document}
