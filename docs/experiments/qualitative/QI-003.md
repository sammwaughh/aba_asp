# QI-003 — Scaled noisy motifs with parent-position controls

## Status

`proposed | planned | implemented | run | analysed | reported`

Current status: `run (computational-feasibility attempt; superseded for evidence by QI-004)`. The `n=100` run repeatedly timed out (cat3/cont3) or hit the binary all-zero-positive encoding limitation (binary), with 0 cells solved before manual abort. Config/fixtures/partial outputs are preserved unchanged; the feasible follow-up is **QI-004** (`QI004_scaled_motifs_n20`, `n=20`, `prolog_timeout_s=300`). See `QI003_scaled_motifs/run_log.md` and `QI004_scaled_motifs_n20/`.

## Purpose

QI-003 scales the motif parent-recovery investigation to `n=100` rows per cell with mild stochastic noise (so the data look realistic rather than degenerate) and, crucially, adds **parent-position controls** that break the x0/first-column confound identified in QI-001. For the chain and fork motifs both orientations are generated: the true direct parent of `x2` sits in column `x0` for some cells and in column `x1` for others. A learner that simply prefers `x0` will therefore fail the `*_x1parent` cells, separating "prefers the true parent" from "prefers x0".

This is an ABA Learning-based parent-set recovery investigation, NOT Russo-style Causal ABA.

## Research question

> With `n=100` noisy samples from 3-node motifs, and the true direct parent of `x2` placed in either column, does the current ABA Learning bridge recover the true parent(s) of `x2` regardless of column position, and how does recovery depend on data mode (binary / categorical-3 / continuous-3)?

## Theoretical motivation

- **ABA foundations:** assumptions, contraries, attacks, extensions.
- **ABA Learning / ABALearn:** transformation-rule learning from BK + E+/E-.
- **Causal ABA / argumentative causal discovery:** Russo-style machinery — NOT used here.
- **Current `aba_asp/causal` implementation:** target-wise learned-rule / parent-set recovery over `handcrafted_table` fixtures.

The confound logic: in QI-001 the true parent coincided with the first column for fork and opposed it for chain, so "found the cause" and "preferred x0" made identical predictions. Varying the parent's column position makes the two hypotheses distinguishable.

## Relation to ABA Learning

- BK built from non-target columns; target `x2` excluded.
- E+/E- fixed by the fixture (positive class = `x2 == 1` binary, `x2 == 2` cat3, `x2 >= 0` cont3).
- Target predicate: `x2`.
- Learner config: default non-deterministic folding, `folding_steps: 15`.

## Relation to Causal ABA

Tests whether learned ABA rule bodies recover causal-parent-like structure under scale, noise, and position controls. Does NOT test full Russo-style Causal ABA.

## Implementation scope

### Code/config paths

- Config path: `causal/configs/experiments/QI003_scaled_motifs.yaml`
- Fixtures: `causal/experiments/handcrafted_qi003.py`
- Fixture registration: `causal/experiments/handcrafted.py`
- Summary script: `causal/scripts/motif_recovery_summary.py`
- Generated summary: `docs/experiments/qualitative/QI003_scaled_motifs_summary.md`
- Output path: `causal/outputs/aba_learning/grid/QI003_scaled_motifs/`

### Files changed

- `causal/experiments/handcrafted_qi003.py` (new)
- `causal/experiments/handcrafted.py` (register QI-003 builders)
- `causal/configs/experiments/QI003_scaled_motifs.yaml` (new)
- `causal/tests/test_qi003_fixtures.py` (new)

## Dataset / data-generating process

### Source

- handcrafted fixture; programmatic `n=100` generators with a fixed per-fixture RNG seed (deterministic). The `handcrafted_table` path ignores `cell.seed`, so determinism comes from the builder seed.

### Graph / structure and parent-position tracking

```text
qi003_chain_x1parent : x0=root,   x1=parent  -> edges (0,1),(1,2)  parents(x2)={x1}
qi003_chain_x0parent : x1=root,   x0=parent  -> edges (1,0),(0,2)  parents(x2)={x0}
qi003_fork_x0parent  : x0=cause,  x1=sibling -> edges (0,1),(0,2)  parents(x2)={x0}
qi003_fork_x1parent  : x1=cause,  x0=sibling -> edges (1,0),(1,2)  parents(x2)={x1}
qi003_collider       : x0,x1 both parents     -> edges (0,2),(1,2)  parents(x2)={x0,x1}
```

5 structural configs x 3 data modes = 15 cells.

### Sample sizes

- `n = 100` per cell.

### Seeds

- Deterministic per-fixture internal seed (`_BASE_SEED + i*10 + j`); grid `seed: [0]`.

### Noise / stochasticity

- chain: intermediate flips from root with prob ~0.2 (breaks collinearity); target flips from parent with prob ~0.1 (binary/cat3) or Gaussian noise (cont3).
- fork: sibling noisier (~0.2) than the x2 target (~0.1) relative to the cause.
- collider: independent parents; `x2 = OR` (binary), `max` (cat3), or sum + Gaussian (cont3).

## Encoding

### Target variable(s)

| Target | True parents | Target type | Reason for inclusion |
|---|---|---|---|
| `x2` | per-fixture (see tracking table) | has-parents / collider | the recovery target |

### Background knowledge

- Target excluded from BK: `yes`
- Binary features (`{0,1}`): bare predicates `x0(A)`
- 3-valued discrete features: value predicates `x0_val_v(A)`
- Continuous features: bin predicates `x0_binK(A)` (`bins=3`, `bin_strategy=uniform`)

## Commands run

```bash
# Prolog-free validation (implemented stage)
python -m pytest causal/tests/test_qi003_fixtures.py -q
python -m causal.experiments.run_grid --config causal/configs/experiments/QI003_scaled_motifs.yaml --dry-run   # 15 cells
python causal/scripts/motif_recovery_summary.py --experiment QI003_scaled_motifs   # pending summary

# ABA Learning run (NOT yet executed):
# python -m causal.experiments.run_grid --config causal/configs/experiments/QI003_scaled_motifs.yaml --no-resume
```

## Artefact paths

| Artefact | Path | Notes |
|---|---|---|
| Config | `causal/configs/experiments/QI003_scaled_motifs.yaml` | 15 cells |
| Output root | `causal/outputs/aba_learning/grid/QI003_scaled_motifs/` | pending run |
| Summary markdown | `docs/experiments/qualitative/QI003_scaled_motifs_summary.md` | regenerate via script |
| Operational dossier | `docs/experiments/qualitative/QI003_scaled_motifs/` | README incl. parent-position tracking table |

## Outcome summary

Pending the ABA Learning run.

## Claims supported

- (Pending run.)

## Claims not supported

- Full Causal ABA; causal discovery; DAG recovery; necessarily-causal rule direction.

## Report relevance

- Interim Experimentation / Progress: the scaled, confound-controlled evidence; the headline `clean_recovery` rate by data mode and by parent position.

## Next decision

Run QI-003 through ABA Learning, regenerate the summary, then fill the dossier `interpretation.md`. The `*_x1parent` vs `*_x0parent` comparison is the decision-critical confound check.
